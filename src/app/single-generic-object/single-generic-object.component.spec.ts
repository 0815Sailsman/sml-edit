/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { SingleGenericObjectComponent } from './single-generic-object.component';

describe('SingleGenericObjectComponent', () => {
  let component: SingleGenericObjectComponent;
  let fixture: ComponentFixture<SingleGenericObjectComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [SingleGenericObjectComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(SingleGenericObjectComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
