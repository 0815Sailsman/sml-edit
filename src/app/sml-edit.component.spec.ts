/*
import { TestBed } from '@angular/core/testing';
import { SmlEditComponent } from './sml-edit.component';

describe('AppComponent', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [SmlEditComponent],
    }).compileComponents();
  });

  it('should create the app', () => {
    const fixture = TestBed.createComponent(SmlEditComponent);
    const app = fixture.componentInstance;
    expect(app).toBeTruthy();
  });

  it(`should have the 'SML-edit' title`, () => {
    const fixture = TestBed.createComponent(SmlEditComponent);
    const app = fixture.componentInstance;
    expect(app.title).toEqual('SML-edit');
  });

  it('should render title', () => {
    const fixture = TestBed.createComponent(SmlEditComponent);
    fixture.detectChanges();
    const compiled = fixture.nativeElement as HTMLElement;
    expect(compiled.querySelector('h1')?.textContent).toContain('Hello, SML-edit');
  });
});
*/
