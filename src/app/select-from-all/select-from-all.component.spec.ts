/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { SelectFromAllComponent } from './select-from-all.component';

describe('SelectomFromAllComponent', () => {
  let component: SelectFromAllComponent;
  let fixture: ComponentFixture<SelectFromAllComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [SelectFromAllComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(SelectFromAllComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
